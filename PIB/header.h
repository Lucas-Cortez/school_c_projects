typedef struct {
    int ano;
    float pib; 
} dados;

// Declaracao das funcoes
void quicksort(dados*, float, float);
void quicksortdois(dados *a, int, int);
float buscaBinaria(dados*, float, float, float);
